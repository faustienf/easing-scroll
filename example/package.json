{
  "name": "example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter easing-scroll run build && vite",
    "check-types": "pnpm --filter easing-scroll run build && tsc -b",
    "build": "pnpm --filter easing-scroll run build && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "easing-scroll": "workspace:*",
    "@codesandbox/sandpack-react": "^2.20.0",
    "@codesandbox/sandpack-themes": "^2.0.21",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react-swc": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}