pnpm run check-types
pnpm test

# The example consumes the built package through the workspace link, so dist/
# has to exist before its type check can resolve `easing-scroll`.
pnpm run build
pnpm --filter example run check-types
